/**
 * App shell header — use with breadcrumbs inside dashboard routes.
 * Marketing pages use `MarketingHeader` directly (no breadcrumbs on landing).
 */
export { MarketingHeader as SiteHeader } from "@/components/marketing-header";
